# Environment variables applied on every deploy.
# Non-secret only. For DATABASE_URL use Secrets Manager (see README or console).
# CLIENT_ORIGIN = frontend origin (Amplify URL or your frontend custom domain, no trailing slash).
option_settings:
  aws:elasticbeanstalk:application:environment:
    CLIENT_ORIGIN: "https://buzzerfly.org"
